#!/bin/bash
# Todo CLI ラッパースクリプト
# 使い方: ./todo <command> [args...]

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export PYTHONPATH="${SCRIPT_DIR}:${PYTHONPATH}"

python3 -m todo_cli.main "$@"
